Lite Code Agent - Ralph Progress Log
Project: Lite Code Agent
Branch: ralph/lite-code-agent
========================================

[2026-06-02] US-001 项目脚手架与配置加载 — 完成
- 创建 package.json：type=module，scripts(start/dev/typecheck)，依赖 @langchain/langgraph@0.2.74、@langchain/core@0.3.80、@langchain/anthropic@0.3.34、dotenv；devDeps tsx@4.22.4、typescript@5.9.3、@types/node。
- 创建 tsconfig.json：ES2022 + ESNext + Bundler 解析，strict 全开，noUnusedLocals/Params，include src/**。
- 实现 src/config.ts：loadConfig() 合并 .env 与 config.json（优先级 env > 文件 > 默认）；强类型 AppConfig（provider/workdir/超时/读取行数/输出字节预算/maxIterations/langfuse）；apiKey 缺失时抛明确错误；isLangfuseEnabled() 判定监控开关；ensureLitecodeDir() 启动时自动创建 .litecode/。
- 实现 src/index.ts：入口，加载配置 → 建 .litecode/ → 打印启动概览；配置错误时可读提示并 exit(1)。
- 创建 .env.example、config.example.json、.gitignore（忽略 node_modules/dist/.env/config.json/.litecode）。
- 默认 model 设为 claude-sonnet-4-6。
- 验收：`npm run typecheck` 通过；无 apiKey 启动报错退出 1；有 apiKey 正常启动并创建 .litecode/。
- 代码全程中文注释。

[2026-06-03] US-002 可切换的 LLM Provider 抽象（默认 Anthropic）— 完成
- 实现 src/provider.ts：createChatModel(config) 工厂，按 provider.type 分发；返回统一的 BaseChatModel。
- anthropic 分支用 ChatAnthropic：apiKey/model 来自配置，baseURL 映射到 anthropicApiUrl（仅在配置时传），maxTokens 默认 4096。
- openai/ollama 为扩展点，命中时抛明确「暂未实现」错误；switch default 用 never 做穷尽性检查。
- src/index.ts 接入工厂：创建模型并校验 model.bindTools 存在（确认支持工具调用），启动概览新增「工具调用支持」行；模型初始化失败时 exit(1)。
- 验收：typecheck 通过；anthropic 正常启动且工具调用支持=是；PROVIDER_TYPE=openai 时报「暂未实现」并退出 1。
- 代码全程中文注释。

[2026-06-03] US-003 只读工具（文件读取分页 + 目录列举）— 完成
- 新增直接依赖 zod（用于工具入参 schema）。
- src/security/path.ts：resolveSafePath() 把相对/绝对路径解析后校验是否在 workdir 内，越界抛 PathAccessError；isPathInside() 布尔版。供所有文件工具复用。
- src/tools/types.ts：ToolLevel(read/write/execute) 与 ToolSpec(tool+level)。
- src/tools/readFile.ts：read_file(path, offset?, limit?)，默认 limit=config.readFileMaxLines；截断时尾部标注剩余行数与 offset 续读；处理 ENOENT/EISDIR/越界为可读错误。level=read。
- src/tools/listDir.ts：list_dir(path?)，目录排前加 "/"，超过 200 项截断并提示；处理 ENOENT/ENOTDIR/越界。level=read。
- src/tools/index.ts：createTools(config) 聚合带级别的工具数组，后续工具在此追加。
- 验收：typecheck 通过；临时脚本验证分页/续读/越界拒绝/文件不存在/目录列举/目录越界均符合预期（脚本已删除）。
- 代码全程中文注释。

[2026-06-03] US-004 整文件写入工具（write_file，需授权）— 完成
- src/tools/types.ts：ToolSpec 增加可选 preview(args) 钩子，用于授权前展示操作详情（US-007 调用）。
- src/tools/writeFile.ts：write_file(path, content)，路径校验、自动建父目录、区分新建/覆盖并报告行数变化；preview() 展示「新建/覆盖 + 路径 + 内容前 50 行摘要」；level=write。
- tools/index.ts 注册 write_file。
- 注：授权拦截的「询问—等待」交互在 US-007 实现，本故事只做工具本体 + preview 钩子。
- 验收：typecheck 通过；临时脚本（临时目录）验证 preview/新建/覆盖/越界拒绝（越界文件未创建）均符合预期，脚本已删除。
- 代码全程中文注释。

[2026-06-03] US-005 基于 diff 的局部编辑工具（edit_file，需授权）— 完成
- src/util/diff.ts：formatReplaceDiff(old,new) 把替换格式化为 -/+ 前缀的可读 diff。
- src/tools/editFile.ts：edit_file(path, old_string, new_string)，路径校验；old_string 唯一性校验（countOccurrences：0=未找到、>1=不唯一，均报错且不修改）；唯一时 replace 写回；preview() 用 formatReplaceDiff 展示改动并对不唯一情况附警告；level=write。
- tools/index.ts 注册 edit_file。
- 验收：typecheck 通过；临时脚本验证唯一替换/未找到/不唯一（文件不被破坏）/越界拒绝/preview diff 与警告均符合预期，脚本已删除。
- 代码全程中文注释。

[2026-06-03] US-006 沙箱命令执行工具（run_command，需授权）— 完成
- src/sandbox/exec.ts：runInSandbox(command, opts) 用 spawn(shell:true, cwd 锁定, stdio 忽略 stdin)；超时 setTimeout→SIGKILL；支持 AbortSignal（abort→SIGKILL，供 US-012 复用）；返回 {stdout,stderr,code,signalName,timedOut,aborted,spawnError}，永不 reject。
- src/util/truncate.ts：truncateHeadTail 按字节预算保留头 N 行+尾 M 行（默认 50/50），中间省略占位；单行超长退化为字符硬截断。
- src/tools/runCommand.ts：包装 runInSandbox，从 RunnableConfig 透传 signal；格式化退出码/stdout/stderr（截断），超时/中断状态行；preview 展示完整命令；level=execute。
- tools/index.ts 注册 run_command。
- 验收：typecheck 通过；临时脚本验证 cwd 锁定/非零退出码/命令不存在(127)/大输出头尾截断/超时(SIGKILL)/AbortSignal 中断均符合预期，脚本已删除。
- 代码全程中文注释。

[2026-06-03] US-007 模式匹配授权与 settings.local.json 持久化 — 完成
- src/permissions/settings.ts：LocalSettings(permissions.allow/deny)；loadSettings（不存在则写默认）、saveSettings、addRule（去重+持久化）。
- src/permissions/match.ts：parseRule 解析「工具名(模式)」；matchPattern 把 * 编译为正则做整体匹配（其余字符转义）；matchesAny；generalizeRule（execute=命令首段+ *，write=目录/*）。
- src/permissions/prompter.ts：AuthPrompter 接口（askDecision y/n/a/d、confirmRule）+ readline 默认实现。依赖注入，US-009 将换 Ink 实现。
- src/permissions/manager.ts：authorize(spec,args)=read 放行 > deny 拒绝 > allow 放行 > 询问；a/d 泛化规则经 confirmRule 写回；permissionTarget 提取（command>path>JSON）。拒绝带 reason 供 agent。
- index.ts：启动 loadSettings 并打印 allow/deny 规则数。
- 验收：typecheck 通过；临时脚本（假 prompter）验证 read 放行/a 泛化写 allow/命中 allow 免询问/d 泛化写 deny/命中 deny 拒绝/y 本次允许/n 本次拒绝，持久化与泛化均正确，脚本已删除。
- 代码全程中文注释。

[2026-06-03] US-008 LangGraph Agent 主循环（StateGraph）— 完成
- src/agent/graph.ts：createAgentGraph(deps) 构建并编译图。
  - AgentState：messages（messagesStateReducer 累积）+ iterations（替换，用于迭代保护）。
  - agent 节点：首轮注入 SystemMessage（中文 code agent 定位），model.bindTools 后 invoke，iterations+1。
  - tools 节点：逐 tool_call 查 specByName → manager.authorize 拦截 → 允许则 spec.tool.invoke(args, config)（透传 config 携带 AbortSignal 供 US-012），拒绝/未知工具/出错均以 ToolMessage 回传。
  - 条件边 shouldContinue：无 tool_calls 或 iterations≥maxIterations → END，否则 → tools；tools→agent 成环。
- 验收：typecheck 通过；脚本化假模型（ScriptedModel，bindTools 返回自身）验证 3 场景：正常 read_file 循环到 END（iter=2、工具消息含内容）/ write_file 授权拒绝回传 reason 且 agent 收尾 / max=2 迭代保护停在 iter=2。脚本已删除。
- 代码全程中文注释。

[2026-06-03] US-009 基于 Ink 的 CLI 交互框架 — 完成（交互式 TTY 跑通待用户在终端验证）
- 依赖：ink@5.2.1、react@18.3.1、ink-text-input@6.0.0、@types/react@18.3.30；tsconfig 开启 jsx=react-jsx。
- src/cli/types.ts：Block（user/ai/tool-call/tool-result/info/error）、AuthRequest（decision/confirm + resolve）、Phase。
- src/cli/controller.ts：SessionController extends EventEmitter implements AuthPrompter。
  - promise-bridge：askDecision/confirmRule 发 'auth' 事件并返回 Promise，UI 按键后 resolve。
  - submit()：graph.stream(updates) 增量运行，renderMessage 把 agent/tools 节点更新转成 block 事件；history 跨轮累积实现连续对话。
  - 内部用自身作 prompter 装配 manager + graph，解决 graph→manager→prompter→controller 的循环依赖。
- src/cli/app.tsx：Ink App，订阅 controller 事件渲染 blocks（边框/颜色块状）；idle 显示 TextInput（/exit 退出），auth 用 useInput 捕获 y/n/a/d（confirm 时 y/n），running 显示静态提示（US-010 加动画）。
- index.ts：装配 model/tools/settings/controller，打印 banner 后 startCli。
- 验收：typecheck 通过；装配脚本验证 controller 构建 graph、事件订阅、promise-bridge(askDecision→a) 均正常；非 TTY 运行确认 banner + 输入框渲染（raw mode 报错为非 TTY 预期）。
- 注意：真实 LLM + 交互式终端的「读文件/写文件/跑命令」端到端跑通需在用户终端用有效 key 运行，会消耗网关 token，尚未实跑。
- 代码全程中文注释。

下一步：US-010 思考阶段动画（ink-spinner + 文案轮换 + 已用时计数 + 中断提示文案）。
