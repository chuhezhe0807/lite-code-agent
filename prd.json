{
  "project": "Lite Code Agent",
  "branchName": "ralph/lite-code-agent",
  "description": "轻量级 code agent，使用 LangGraph + TypeScript 实现。参考 Claude Code 的交互模式：CLI 对话、受限 child_process 沙箱执行代码与构建脚本、本地文件读写、Claude Code 式模式匹配授权（持久化到 .litecode/settings.local.json）、默认 Anthropic provider（claude-sonnet-4-6），可选 Langfuse 监控。用于学习 Agent 应用开发。",
  "userStories": [
    {
      "id": "US-001",
      "title": "项目脚手架与配置加载",
      "description": "As a developer, I want 一个可运行的 TypeScript 项目骨架与配置系统 so that 我可以从一个干净的基础上开始构建 agent。",
      "acceptanceCriteria": [
        "初始化 TS 项目（package.json、tsconfig.json、src/），可用 tsx 运行",
        "安装依赖：@langchain/langgraph、@langchain/core、@langchain/anthropic",
        "配置从 config.json 或环境变量加载：provider 类型、apiKey、baseURL、model、工作目录、超时时间、Langfuse 配置（可选）",
        "提供 .env.example 与示例 config.json",
        "启动时自动创建 .litecode/ 目录（若不存在）",
        "代码含中文注释",
        "Typecheck passes"
      ],
      "priority": 1,
      "passes": true,
      "notes": "已完成并提交（commit 9fcfdc8）。typecheck 通过；无 apiKey 报错、有 apiKey 正常启动并创建 .litecode/。"
    },
    {
      "id": "US-002",
      "title": "可切换的 LLM Provider 抽象（默认 Anthropic）",
      "description": "As a developer, I want 一个统一的模型接入层 so that 我可以配置 Anthropic 并在未来切换其他 provider 而不改 agent 主逻辑。",
      "acceptanceCriteria": [
        "定义工厂函数，根据配置返回 LangChain 的 ChatModel 实例",
        "默认 provider 为 Anthropic（@langchain/anthropic），支持配置 apiKey、baseURL、model，默认 model 为 claude-sonnet-4-6",
        "apiKey 缺失时启动给出明确报错；baseURL 可选",
        "provider 层留出扩展点，未来可加 OpenAI / Ollama 而不改 agent 主循环",
        "模型支持 tool calling（bind tools）",
        "代码含中文注释",
        "Typecheck passes"
      ],
      "priority": 2,
      "passes": true,
      "notes": "已完成。createChatModel 工厂默认 ChatAnthropic，支持 apiKey/baseURL/model，openai/ollama 留扩展点，bindTools 校验通过。"
    },
    {
      "id": "US-003",
      "title": "只读工具：文件读取（分页）与目录列举",
      "description": "As a user, I want agent 能读取文件和列出目录 so that 它能理解我的项目结构和代码。",
      "acceptanceCriteria": [
        "实现 read_file(path, offset?, limit?) 工具：默认只读前 2000 行，支持 offset/limit 分页",
        "截断时尾部标注剩余行数与续读方式（如 offset=...）",
        "实现 list_dir(path) 工具：条目过多时返回总数 + 前 N 条并提示用更具体路径",
        "路径经工作目录白名单校验，越界路径返回明确错误而非读取",
        "只读工具默认放行（不需授权）",
        "代码含中文注释",
        "Typecheck passes"
      ],
      "priority": 3,
      "passes": true,
      "notes": "已完成。resolveSafePath 路径白名单校验；read_file offset/limit 分页+截断标注；list_dir 目录排前+超 200 项截断。临时脚本验证越界/不存在/分页均通过。"
    },
    {
      "id": "US-004",
      "title": "工具：整文件写入（需授权）",
      "description": "As a user, I want agent 能创建和修改文件 so that 它能帮我写代码，但我要先批准。",
      "acceptanceCriteria": [
        "实现 write_file(path, content) 工具：写入/覆盖文件",
        "写入前显示将要写入的路径与内容摘要（或 diff）",
        "路径越界（超出工作目录白名单）直接拒绝",
        "工具标注为「写入」授权级别",
        "代码含中文注释",
        "Typecheck passes"
      ],
      "priority": 4,
      "passes": true,
      "notes": "已完成。write_file 路径校验+自动建父目录+新建/覆盖行数报告；ToolSpec 增加 preview 钩子展示路径与内容摘要。授权交互留待 US-007。临时脚本验证新建/覆盖/越界拒绝通过。"
    },
    {
      "id": "US-005",
      "title": "工具：基于 diff 的局部编辑（需授权）",
      "description": "As a user, I want agent 能对现有文件做局部修改而非整文件覆盖 so that 改动更精确、可见、可控。",
      "acceptanceCriteria": [
        "实现 edit_file(path, old_string, new_string) 工具：精确替换片段",
        "old_string 在文件中不唯一或不存在时返回明确错误，不做修改",
        "修改前展示 diff（前后对比）",
        "路径越界直接拒绝",
        "工具标注为「写入」授权级别",
        "代码含中文注释",
        "Typecheck passes"
      ],
      "priority": 5,
      "passes": true,
      "notes": "已完成。edit_file 精确替换 + old_string 唯一性校验（0/多个均报错不改）；formatReplaceDiff 生成 -/+ diff 供 preview。临时脚本验证替换/未找到/不唯一/越界/预览警告通过。"
    },
    {
      "id": "US-006",
      "title": "工具：沙箱命令执行（child_process + 限制，需授权）",
      "description": "As a user, I want agent 能在受限沙箱中运行命令和构建脚本 so that 它能执行测试、构建、安装依赖等任务而不威胁我的系统。",
      "acceptanceCriteria": [
        "实现 run_command(command) 工具：通过 child_process spawn 执行",
        "cwd 锁定为配置的工作目录",
        "强制执行超时（可配置，默认 30s），超时则杀掉进程并返回错误",
        "捕获并返回 stdout、stderr，始终完整返回 exit code",
        "输出按字节预算截断（默认 30KB）：保留头部 N 行 + 尾部 M 行，中间用省略占位",
        "工具标注为「执行」授权级别",
        "代码含中文注释",
        "Typecheck passes"
      ],
      "priority": 6,
      "passes": true,
      "notes": "已完成。runInSandbox 用 spawn(shell+cwd锁定)，超时 SIGKILL，支持 AbortSignal；truncateHeadTail 头尾截断；run_command 透传 signal、格式化退出码/输出。临时脚本验证 cwd/退出码/超时/中断/截断通过。"
    },
    {
      "id": "US-007",
      "title": "模式匹配授权与 settings.local.json 持久化",
      "description": "As a user, I want 用「工具名 + 参数模式」的规则来授权（参考 Claude Code）并能把决定持久化 so that 我能精细控制哪些操作免询问，下次会话也生效。",
      "acceptanceCriteria": [
        "授权规则采用「工具名(参数模式)」格式，支持通配符 *（如 Bash(npx tsc *)、WriteFile(src/*)）",
        "规则存于 .litecode/settings.local.json 的 permissions.allow 与 permissions.deny 列表",
        "匹配逻辑：命中 deny 直接拒绝；命中 allow 免询问放行；都不命中则提示用户",
        "只读工具默认放行，无需出现在 allow 列表",
        "提示用户时展示工具名+具体参数；支持 y（本次允许）/ n（本次拒绝）/ a（始终允许写入 allow）/ d（始终拒绝写入 deny）",
        "选 a/d 时将当前调用泛化为可复用模式规则并写回 settings.local.json",
        "启动时文件不存在则创建默认结构，存在则加载已记住的规则",
        "拒绝时将拒绝信息作为工具结果回传给 agent 而非崩溃",
        "代码含中文注释",
        "Typecheck passes"
      ],
      "priority": 7,
      "passes": true,
      "notes": "已完成。settings.local.json 读写+默认创建；工具名(模式) * 通配匹配；authorize: read放行>deny>allow>询问(y/n/a/d)；a/d 泛化规则经确认写回；prompter 接口注入(readline默认,US-009换Ink)。假 prompter 脚本验证全流程通过。"
    },
    {
      "id": "US-008",
      "title": "LangGraph Agent 主循环（StateGraph）",
      "description": "As a developer, I want 用 LangGraph 的 StateGraph 编排 agent 的思考-行动循环 so that 我能清楚理解 agent 的状态流转。",
      "acceptanceCriteria": [
        "定义图状态（messages 累积）",
        "节点：agent（调用 LLM，可能产生 tool_calls）、tools（执行工具，含授权拦截）",
        "条件边：若 LLM 返回 tool_calls 则进入 tools 节点，否则结束",
        "tools 执行完回到 agent 节点形成循环",
        "有最大迭代次数保护防止无限循环",
        "代码含中文注释",
        "Typecheck passes"
      ],
      "priority": 8,
      "passes": true,
      "notes": "已完成。StateGraph：agent 节点(bindTools+系统提示)、tools 节点(授权拦截+执行+拒绝/出错回传ToolMessage)、条件边(无tool_calls或超迭代→END)、messages+iterations 状态。假模型脚本验证正常循环/授权拒绝回传/max 迭代保护通过。"
    },
    {
      "id": "US-009",
      "title": "基于 Ink 的 CLI 交互框架",
      "description": "As a user, I want 一个用 Ink 渲染的命令行界面 so that 我能像用 Claude Code 一样输入任务、看到美化的过程展示。",
      "acceptanceCriteria": [
        "引入 ink、react、ink-text-input 依赖，用 Ink App 渲染主界面",
        "REPL 循环：输入任务（ink-text-input 或 useInput）→ 运行 agent → 渲染过程 → 等待下一条",
        "工具调用渲染为带边框/颜色的块：工具名 + 参数（高亮）+ 结果（截断后）",
        "渲染层从 console.log 改为状态驱动的 React 组件；agent 事件通过回调/事件流推给 UI",
        "agent 运行期间禁用输入框，结束后恢复",
        "授权提示在 Ink 界面内交互（对接 US-007）",
        "支持退出命令（如 /exit）",
        "手动跑通：在示例目录让 agent 读文件、写新文件、运行一个构建/测试命令",
        "代码含中文注释",
        "Typecheck passes"
      ],
      "priority": 9,
      "passes": true,
      "notes": "代码完成。SessionController(EventEmitter+AuthPrompter promise-bridge) 桥接 graph.stream 与 Ink；app.tsx 块状渲染+TextInput+useInput 授权(y/n/a/d)+/exit。装配/事件/bridge 脚本验证通过；非TTY确认banner+输入框渲染。端到端真实LLM交互需用户在终端用有效key跑通(消耗token,未实跑)。"
    },
    {
      "id": "US-010",
      "title": "思考阶段动画",
      "description": "As a user, I want 在 agent 等待 LLM 响应时看到动态指示 so that 我知道它在工作而不是卡死（参考 Claude Code）。",
      "acceptanceCriteria": [
        "用 ink-spinner 显示旋转动画 + 状态文案（如「思考中…」，可在一组词间轮换）",
        "显示已用时计数（秒，每秒刷新，如「思考中… (3s)」）",
        "显示可中断提示文案（如「按 Esc 中断」），中断功能由 US-012 实现",
        "LLM 返回后动画消失，进入下一阶段渲染",
        "仅用于「等待 LLM」阶段，仍是分步骤块状、非流式 token",
        "代码含中文注释",
        "Typecheck passes"
      ],
      "priority": 10,
      "passes": true,
      "notes": "已完成。ThinkingIndicator=ink-spinner(dots)+文案轮换(思考中/分析中/规划中/处理中,每2s)+已用时计数(每秒)+「按Esc中断」提示；running 阶段挂载。typecheck+导入解析通过；动画效果需真实终端观察。"
    },
    {
      "id": "US-011",
      "title": "Todo 列表实时勾选",
      "description": "As a user, I want agent 维护一个 todo 列表并在 UI 中实时勾选 so that 我能看到任务拆解与进度（参考 Claude Code TodoWrite）。",
      "acceptanceCriteria": [
        "新增 update_todos 工具：agent 主动传入 todo 项及其状态（pending/in_progress/completed）",
        "todo 状态进入 LangGraph 图状态，与对话消息一起流转",
        "Ink 渲染列表，状态字形：○ 待办 / ◐（或 spinner）进行中 / ✓ 已完成（绿色）",
        "agent 更新某项状态时列表原地刷新，不重复打印整段",
        "只有 agent 调用 update_todos 才产生 todo（不由 UI 自动推断）",
        "代码含中文注释",
        "Typecheck passes"
      ],
      "priority": 11,
      "passes": true,
      "notes": "已完成。update_todos 工具(level=read 免授权)；图状态增 todos，tools 节点识别该调用写回；controller 发 'todos' 事件；TodoView 渲染 ○/◐/✓。脚本化模型验证图状态与事件流通过。授权交互另改为方向键选择(SelectList)。"
    },
    {
      "id": "US-012",
      "title": "Esc 中断运行中的 agent",
      "description": "As a user, I want 按 Esc 中断正在进行的 agent 运行 so that 我能在它跑偏或耗时过长时及时叫停。",
      "acceptanceCriteria": [
        "用 AbortController/AbortSignal 贯穿 agent 主循环（LLM 调用与工具执行均可被取消）",
        "Ink 界面监听 Esc 键（useInput），触发 abort",
        "中断后停止当前迭代，向用户显示「已中断」并恢复输入框等待下一条",
        "中断时正在执行的 run_command 子进程被杀掉（复用超时杀进程逻辑）",
        "中断不导致程序崩溃，状态可继续下一轮对话",
        "代码含中文注释",
        "Typecheck passes"
      ],
      "priority": 12,
      "passes": true,
      "notes": "已完成。controller 每轮建 AbortController，signal 透传 graph.stream 贯穿 LLM/工具/子进程；abort() + app useInput 监听 Esc；中断显示「已中断」、不污染 history。脚本化模型+sleep 验证 700ms 中断杀子进程、恢复 idle 通过。Esc 实体按键需真实终端。"
    },
    {
      "id": "US-013",
      "title": "可选 Langfuse 监控集成",
      "description": "As a developer, I want 把 agent 的调用链上报到 Langfuse so that 我能观测每次 LLM 调用与工具执行，便于调试和学习。",
      "acceptanceCriteria": [
        "通过配置（publicKey、secretKey、baseURL）启用 Langfuse；三者缺失时自动跳过且程序正常运行",
        "启用时用 langfuse-langchain 的 CallbackHandler 将 LLM 调用与工具执行链路上报",
        "未配置 Langfuse 不引入运行时错误、不阻塞主流程",
        "README 说明如何配置 Langfuse（可选）",
        "代码含中文注释",
        "Typecheck passes"
      ],
      "priority": 13,
      "passes": true,
      "notes": "已完成。createLangfuseHandler 仅在 publicKey+secretKey 齐全时建 CallbackHandler，否则 undefined；controller 挂 callbacks 并 flushAsync。docker-compose.yml 本地自托管。工厂测试通过；未配置不报错。实际上报需起 compose+建项目拿 key。"
    },
    {
      "id": "US-014",
      "title": "文档与示例",
      "description": "As a learner, I want 一份说明文档和可复现的示例 so that 我能理解每个模块并自己扩展。",
      "acceptanceCriteria": [
        "README.md：安装、配置、运行步骤，架构说明（状态图、工具、授权流、Ink UI、Langfuse）",
        "提供 examples/ 示例工作目录供 agent 操作演示",
        "关键模块（图、工具、授权、provider、UI）有中文解释性注释",
        "Typecheck passes"
      ],
      "priority": 14,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-015",
      "title": "降低 token 消耗（输入/输出优化）",
      "description": "As a 维护者, I want 在不影响功能的前提下减少每轮对话的 token 消耗 so that 长会话/多轮工具调用更省钱。",
      "acceptanceCriteria": [
        "收紧系统提示：指示模型不复述工具结果、只给精简最终结论，减少冗余输出 token",
        "探测 LLM 网关（如 LiteLLM 代理）是否支持 Anthropic prompt caching；支持则对系统提示+工具定义+稳定历史加 cache_control 以省输入 token",
        "工具输出预算（commandOutputMaxBytes / readFileMaxLines）可调小；可选：历史中用过的大工具结果做截断/压缩，避免每轮重发",
        "在代码/文档中明确：UI 的展示裁剪（clampCount）仅影响显示，不减少发给模型的 token",
        "Typecheck passes"
      ],
      "priority": 15,
      "passes": false,
      "notes": "后续优化项。要点：工具结果是下一轮的输入 token（非输出）；工具调用参数是必需输出；模型每步解说 prose 是可省的输出 token；多轮里重发全部历史是输入 token 大头，prompt caching 是最大杠杆。"
    }
  ]
}
